[package]
name = "mcp-genai-glue"
version = "0.1.0"
edition = "2024"
description = "Glue crate bridging MCP servers (rmcp) with genai tool-calling."
license = "MIT"

[dependencies]
genai = "0.6.5"
rmcp = { version = "3.1.1", default-features = false, features = ["client"] }
serde_json = "1"
thiserror = "2"
